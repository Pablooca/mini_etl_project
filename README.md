# mini_etl_project
Robust Batch ETL pipeline implemented in Python. Extracts air quality data via REST API, performs vectorized data cleansing with Pandas, and executes idempotent loads (Upsert) into SQLite with structured logging
